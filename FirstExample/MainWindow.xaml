<Window x:Class="FirstExample.SumW"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FirstExample"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <TextBlock Text="Сумма" 
                   FontSize="20"
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15"/>

        
        <TextBlock Grid.Row="1" Text="Введите число:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="InputTextBox" Height="30" 
                 Margin="0,0,0,15" Padding="5" FontSize="14"/>

        
        <Button Grid.Row="3" x:Name="CalculateButton" Content="Вычислить сумму" 
                Height="35" FontSize="14" Margin="0,0,0,15"
                Click="CalculateButton_Click"/>

       
        <TextBlock Grid.Row="4" Text="Результат:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" x:Name="ResultTextBox" Height="30" 
                 IsReadOnly="True" Padding="5" FontSize="14" 
                 Background="#FFF0F0F0"/>

        
        <ProgressBar Grid.Row="6" x:Name="ProgressBar" Height="10" 
                     Margin="0,10,0,0" Visibility="Collapsed"/>
    </Grid>
</Window>
