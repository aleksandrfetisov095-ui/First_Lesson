<Window x:Class="ParallelCalcul.View.SumWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParallelCalcul.View"
        mc:Ignorable="d"
        Title="Сумма" Height="300" Width="350">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вычисление суммы" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Введите число:"/>
        <TextBox Grid.Row="2" Text="{Binding InputNumber, UpdateSourceTrigger=PropertyChanged}" 
                 Height="25" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Вычислить сумму" 
                    Command="{Binding CalculateCommand}"
                    Width="120" Height="30" Margin="5"/>
            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Width="80" Height="30" Margin="5"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="Результат:" Margin="0,10,0,5"/>
        <TextBox Grid.Row="5" Text="{Binding Result, StringFormat=F2}" 
                 IsReadOnly="True" Height="25" Background="#FFF0F0F0"/>
    </Grid>
</Window>
